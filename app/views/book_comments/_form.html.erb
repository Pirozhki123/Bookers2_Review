<%= form_with(model:[book, book_comment], local: true) do |f| %> <!--book,book_commentに格納するフォームを作成-->
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに", class: "w-100" %> <!--コメント入力フォーム-->
  <%= f.submit "送信する", class: "btn btn-lg btn-base-1 mt-20 pull-right" %> <!--送信ボタンを表示-->
<% end %>